# TodoList

## Endpoint

# get data
# router.get("/", getTodos)

# post data
# router.post("/", postTodo)

# update data
# router.put("/:id", updateTodo)

# delete data
# router.delete("/:id", deleteTodo)